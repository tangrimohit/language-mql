#include "stdio.h"
#include "string.h"
int main(int argc, char const *argv[]) {
        char str[1000];
        int i=0;
        scanf("%s",str);
        for ( i = 0; i <strlen(str); i++) {
                /* code */
                if (int(str[i])) {
                        /* code */
                }
        }

        return 0;
}
